# Bloquea el acceso web directo a esta carpeta (conexion.php, .env no deben ser accesibles).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
